<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title> Ting's Animal Zoo </title>
  </head>
  <body>
    <body>
  <main>
    <h2> All of the Animals </h2>
    <ul>
      <% @animals.each do |animal| %>
        <li><%=  "Name: #{animal.name}, Species: #{animal.species}, Age: #{animal.age}" %></li>
      <% end %>
    </ul>
  </main>
</body>
  </body>
</html>
