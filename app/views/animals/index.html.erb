<h1>All Animals</h1>
<%= link_to "Create a new animal", animals_new_path %>

<ul id="animals">
  <% @animals.each do |animal| %>
  <li>
    <%= link_to animal.name, animal_path(animal) %><br />
    <img src=<%=animal.image%>><br />
  </li>
  <% end %>

</ul>
