<h1> The Animals </h1>
<ul>
  <% @animals.each do |animal| %>
  <li>
    <%= link_to animal.name, animal_path(animal.id) %> -
    <%=  animal.species %>
  </li>
  <% end %>
  <%= button_to "Create New Animal", new_animal_path, method: :get %>
<ul>
