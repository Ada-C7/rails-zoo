<!DOCTYPE html>
<html>
  <head>
    <title>Rails Zoo<%= @title %></title>
    <%= csrf_meta_tags %>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <header>
      <h1><%= link_to "Rails Zoo", root_path %></h1>
      <nav>
        <ul>
          <li class="<%= 'selected' if @animals_selected %>">
            <%= link_to "Animals", animals_path %>
          </li>
          <li class="<%= 'selected' if @new_selected %>">
            <%= link_to "Add Animal", new_animal_path %>
          </li>
        </ul>
      </nav>
    </header>
    <%= yield %>
    <footer>
      <p>&copy; <%= Time.current.year %></p>
    </footer>
  </body>
</html>
